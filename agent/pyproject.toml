[project]
name = "solenegpt"
version = "0.0.1"
description = "SoleneGPT — voice-first Dev3pack mentor"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[cartesia,deepgram,elevenlabs,google,openai,silero,turn-detector]~=1.0",
    "openai>=1.76.0",
    "python-dotenv>=1.1.0",
    "httpx>=0.27.0",
]

[tool.setuptools.packages.find]
include = ["voice_agent*"]
